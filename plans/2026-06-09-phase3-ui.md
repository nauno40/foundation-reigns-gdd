# Phase 3 — UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the complete Godot UI — card screen with swipe, resource bars, death screen, galaxy map, and the main game loop wiring all modules together.

**Architecture:** Each UI component is an independent scene (`*.tscn`) with an attached script. `UIManager.gd` owns the swipe input logic. `Main.gd` orchestrates the game loop by calling the Phase 2 core modules. No game logic lives in UI scripts — they only display state and emit signals.

**Tech Stack:** Godot 4.x, GDScript 4, Control nodes, SwipeDetector (custom InputHandler)

**Prerequisite:** Phase 2 complete — all core modules pass their tests.

---

## File Structure

```
~/foundation-reigns/
├── scenes/
│   ├── Main.tscn              ← scène principale, orchestration
│   ├── CardScreen.tscn        ← affichage d'une carte + swipe
│   ├── ResourceBars.tscn      ← 4 barres de ressources
│   ├── DeathScreen.tscn       ← écran de mort
│   └── GalaxyMap.tscn         ← carte galactique statique
├── src/
│   ├── core/                  ← (Phase 2, inchangé)
│   ├── ui/
│   │   ├── CardScreen.gd
│   │   ├── ResourceBars.gd
│   │   ├── DeathScreen.gd
│   │   ├── GalaxyMap.gd
│   │   └── SwipeDetector.gd
│   └── main/
│       └── Main.gd            ← boucle principale
└── assets/
    ├── portraits/             ← sprites personnages (placeholders PNG)
    └── backgrounds/           ← fonds d'écran (placeholders)
```

---

## Task 1 : SwipeDetector

**Files:**
- Create: `~/foundation-reigns/src/ui/SwipeDetector.gd`

Le SwipeDetector traduit les gestes tactiles ET les drags souris en signaux `swiped_left` / `swiped_right`.

- [ ] **Step 1 : Créer SwipeDetector.gd**

  ```gdscript
  class_name SwipeDetector
  extends Node

  signal swiped_left
  signal swiped_right
  signal swipe_progress(ratio: float)  # -1.0 (left) to 1.0 (right)

  const MIN_SWIPE_DISTANCE = 80.0  # pixels
  const MAX_SWIPE_ANGLE = 45.0     # degrés max d'inclinaison acceptée

  var _drag_start: Vector2 = Vector2.ZERO
  var _is_dragging: bool = false

  func _input(event: InputEvent) -> void:
      if event is InputEventScreenTouch:
          if event.pressed:
              _start_drag(event.position)
          else:
              _end_drag(event.position)

      elif event is InputEventScreenDrag:
          if _is_dragging:
              _update_progress(event.position)

      elif event is InputEventMouseButton:
          if event.button_index == MOUSE_BUTTON_LEFT:
              if event.pressed:
                  _start_drag(event.position)
              else:
                  _end_drag(event.position)

      elif event is InputEventMouseMotion:
          if _is_dragging and Input.is_mouse_button_pressed(MOUSE_BUTTON_LEFT):
              _update_progress(event.position)

  func _start_drag(pos: Vector2) -> void:
      _drag_start = pos
      _is_dragging = true

  func _update_progress(pos: Vector2) -> void:
      var delta = pos - _drag_start
      var ratio = clamp(delta.x / MIN_SWIPE_DISTANCE, -1.0, 1.0)
      swipe_progress.emit(ratio)

  func _end_drag(pos: Vector2) -> void:
      if not _is_dragging:
          return
      _is_dragging = false

      var delta = pos - _drag_start
      var distance = abs(delta.x)
      if distance < MIN_SWIPE_DISTANCE:
          swipe_progress.emit(0.0)
          return

      var angle = abs(rad_to_deg(atan2(delta.y, delta.x)))
      if angle > MAX_SWIPE_ANGLE and angle < 180.0 - MAX_SWIPE_ANGLE:
          swipe_progress.emit(0.0)
          return

      swipe_progress.emit(0.0)
      if delta.x < 0:
          swiped_left.emit()
      else:
          swiped_right.emit()
  ```

- [ ] **Step 2 : Test manuel**

  Créer une scène test temporaire `scenes/TestSwipe.tscn` avec :
  - Un SwipeDetector (Node)
  - Un Label "Swipe here"
  
  Script attaché à la scène :
  ```gdscript
  extends Node

  @onready var detector = $SwipeDetector
  @onready var label = $Label

  func _ready():
      detector.swiped_left.connect(func(): label.text = "← LEFT")
      detector.swiped_right.connect(func(): label.text = "RIGHT →")
      detector.swipe_progress.connect(func(r): label.text = "%.2f" % r)
  ```

  Lancer le projet dans Godot, tester le swipe à la souris.
  Attendu : le label affiche LEFT / RIGHT selon le swipe.

- [ ] **Step 3 : Committer**

  ```bash
  git add src/ui/SwipeDetector.gd
  git commit -m "feat(ui): implement SwipeDetector (touch + mouse)"
  ```

---

## Task 2 : ResourceBars

**Files:**
- Create: `~/foundation-reigns/src/ui/ResourceBars.gd`
- Create: `~/foundation-reigns/scenes/ResourceBars.tscn`

Affiche 4 barres horizontales (Military / Religion / Commerce / Politics).

- [ ] **Step 1 : Créer la scène ResourceBars.tscn**

  Dans Godot, créer `scenes/ResourceBars.tscn` :
  ```
  ResourceBars (HBoxContainer)
  ├── MilitaryBar  (ProgressBar, min=0, max=100, value=50)
  ├── ReligionBar  (ProgressBar, min=0, max=100, value=50)
  ├── CommerceBar  (ProgressBar, min=0, max=100, value=50)
  └── PoliticsBar  (ProgressBar, min=0, max=100, value=50)
  ```

  Propriétés de chaque ProgressBar :
  - `custom_minimum_size`: (20, 120) — barre verticale mince
  - Rotation: 90° pour affichage vertical
  - `fill_mode`: Bottom to Top

- [ ] **Step 2 : Créer ResourceBars.gd**

  ```gdscript
  class_name ResourceBars
  extends HBoxContainer

  const COLOR_NORMAL   = Color(0.3, 0.7, 0.3)   # vert
  const COLOR_WARNING  = Color(0.9, 0.6, 0.1)   # orange
  const COLOR_CRITICAL = Color(0.9, 0.1, 0.1)   # rouge

  const THRESHOLD_CRITICAL_LOW  = 15
  const THRESHOLD_WARNING_LOW   = 25
  const THRESHOLD_WARNING_HIGH  = 75
  const THRESHOLD_CRITICAL_HIGH = 85

  @onready var _bars = {
      "military": $MilitaryBar,
      "religion": $ReligionBar,
      "commerce": $CommerceBar,
      "politics": $PoliticsBar,
  }

  func update(ctx: Context) -> void:
      for resource in _bars:
          var value: int = ctx.get_var(resource, 50)
          _update_bar(_bars[resource], value)

  func _update_bar(bar: ProgressBar, value: int) -> void:
      bar.value = value
      var color = _get_color(value)
      var style = StyleBoxFlat.new()
      style.bg_color = color
      bar.add_theme_stylebox_override("fill", style)

      # Blink animation for critical state
      if value < THRESHOLD_CRITICAL_LOW or value > THRESHOLD_CRITICAL_HIGH:
          _start_blink(bar)
      else:
          _stop_blink(bar)

  func _get_color(value: int) -> Color:
      if value < THRESHOLD_CRITICAL_LOW or value > THRESHOLD_CRITICAL_HIGH:
          return COLOR_CRITICAL
      if value < THRESHOLD_WARNING_LOW or value > THRESHOLD_WARNING_HIGH:
          return COLOR_WARNING
      return COLOR_NORMAL

  func _start_blink(bar: ProgressBar) -> void:
      if not bar.has_meta("blinking"):
          bar.set_meta("blinking", true)
          var tween = create_tween().set_loops()
          tween.tween_property(bar, "modulate:a", 0.3, 0.4)
          tween.tween_property(bar, "modulate:a", 1.0, 0.4)
          bar.set_meta("tween", tween)

  func _stop_blink(bar: ProgressBar) -> void:
      if bar.has_meta("blinking"):
          bar.remove_meta("blinking")
          if bar.has_meta("tween"):
              bar.get_meta("tween").kill()
              bar.remove_meta("tween")
          bar.modulate.a = 1.0
  ```

- [ ] **Step 3 : Test visuel**

  Ajouter `ResourceBars` à une scène test, appeler `update()` avec des valeurs variées :
  - value=10 → barre rouge clignotante
  - value=20 → barre orange
  - value=50 → barre verte
  - value=80 → barre orange
  - value=92 → barre rouge clignotante

- [ ] **Step 4 : Committer**

  ```bash
  git add src/ui/ResourceBars.gd scenes/ResourceBars.tscn
  git commit -m "feat(ui): implement ResourceBars with color thresholds and blink"
  ```

---

## Task 3 : CardScreen

**Files:**
- Create: `~/foundation-reigns/src/ui/CardScreen.gd`
- Create: `~/foundation-reigns/scenes/CardScreen.tscn`

L'écran principal du jeu. Affiche la carte et gère le swipe.

- [ ] **Step 1 : Créer la scène CardScreen.tscn**

  ```
  CardScreen (Control, full screen)
  ├── Background (TextureRect, anchors: full rect)
  ├── Portrait (TextureRect, center-top)
  ├── CardPanel (PanelContainer, center)
  │   ├── QuestionLabel (RichTextLabel)
  │   ├── LeftHint (Label, left edge, hidden initially)
  │   └── RightHint (Label, right edge, hidden initially)
  ├── ResourceBars (instance of ResourceBars.tscn, bottom)
  ├── HUDInfo (HBoxContainer, top)
  │   ├── YearLabel (Label)  ← "An 42"
  │   └── AgeLabel  (Label)  ← "Âge : 38"
  └── SwipeDetector (Node)
  ```

- [ ] **Step 2 : Créer CardScreen.gd**

  ```gdscript
  class_name CardScreen
  extends Control

  signal choice_made(is_left: bool)

  @onready var _background   = $Background
  @onready var _portrait     = $Portrait
  @onready var _question     = $CardPanel/QuestionLabel
  @onready var _left_hint    = $CardPanel/LeftHint
  @onready var _right_hint   = $CardPanel/RightHint
  @onready var _resource_bars = $ResourceBars
  @onready var _year_label   = $HUDInfo/YearLabel
  @onready var _age_label    = $HUDInfo/AgeLabel
  @onready var _swipe_detector = $SwipeDetector

  var _current_card: Dictionary = {}
  var _can_swipe: bool = true

  func _ready() -> void:
      _swipe_detector.swiped_left.connect(_on_swipe_left)
      _swipe_detector.swiped_right.connect(_on_swipe_right)
      _swipe_detector.swipe_progress.connect(_on_swipe_progress)

  func show_card(card: Dictionary, ctx: Context) -> void:
      _current_card = card
      _can_swipe = true

      # Question text (FR priority)
      var question = card.get("question", {})
      _question.text = question.get("FR", question.get("EN", "???"))

      # Answer hints
      var left_answer = card.get("leftAnswer", {})
      var right_answer = card.get("rightAnswer", {})
      var left_title = left_answer.get("title", {})
      var right_title = right_answer.get("title", {})
      _left_hint.text  = "← " + left_title.get("FR", left_title.get("EN", ""))
      _right_hint.text = right_title.get("FR", right_title.get("EN", "")) + " →"
      _left_hint.visible  = false
      _right_hint.visible = false

      # HUD
      _year_label.text = "An %d" % ctx.get_var("year", 1)
      _age_label.text  = "Âge : %d" % ctx.get_var("age", 35)

      # Resource bars
      _resource_bars.update(ctx)

      # Card tilt reset
      _card_panel_tilt(0.0)

  func _on_swipe_left() -> void:
      if not _can_swipe:
          return
      _can_swipe = false
      _animate_swipe_out(-1.0)

  func _on_swipe_right() -> void:
      if not _can_swipe:
          return
      _can_swipe = false
      _animate_swipe_out(1.0)

  func _on_swipe_progress(ratio: float) -> void:
      _card_panel_tilt(ratio)
      _left_hint.visible  = ratio < -0.3
      _right_hint.visible = ratio > 0.3

  func _card_panel_tilt(ratio: float) -> void:
      var card_panel = $CardPanel
      card_panel.rotation = ratio * 0.15
      card_panel.position.x = ratio * 30.0

  func _animate_swipe_out(direction: float) -> void:
      var card_panel = $CardPanel
      var tween = create_tween()
      tween.tween_property(card_panel, "position:x", direction * 600.0, 0.25)
      tween.tween_property(card_panel, "modulate:a", 0.0, 0.15)
      await tween.finished
      choice_made.emit(direction < 0)  # left = true = yes outcome

  func show_reaction(card: Dictionary, is_left: bool) -> void:
      var answer_key = "leftAnswer" if is_left else "rightAnswer"
      var answer = card.get(answer_key, {})
      var reaction = answer.get("reaction", {})
      _question.text = reaction.get("FR", reaction.get("EN", ""))
      # Reset card position
      var card_panel = $CardPanel
      card_panel.position.x = 0
      card_panel.rotation = 0
      card_panel.modulate.a = 1.0
      _left_hint.visible  = false
      _right_hint.visible = false
      _can_swipe = false
  ```

- [ ] **Step 3 : Test visuel**

  Créer une scène test `scenes/TestCardScreen.tscn` :
  ```gdscript
  extends Node

  @onready var card_screen = $CardScreen

  func _ready():
      var fake_ctx = Context.new()
      fake_ctx.initialize_new_reign()
      fake_ctx.set_var("year", 42, true)
      fake_ctx.set_var("age", 38)

      var fake_card = {
          "id": 1001,
          "question": {"FR": "Un espion anacréonien a été capturé. Que faire ?"},
          "leftAnswer": {
              "title": {"FR": "L'emprisonner"},
              "reaction": {"FR": "Il restera dans nos cellules."}
          },
          "rightAnswer": {
              "title": {"FR": "Le relâcher en échange d'informations"},
              "reaction": {"FR": "Un deal risqué mais pragmatique."}
          }
      }

      card_screen.show_card(fake_card, fake_ctx)
      card_screen.choice_made.connect(func(is_left):
          print("Choix : ", "gauche" if is_left else "droite")
          card_screen.show_reaction(fake_card, is_left)
      )
  ```

  Lancer dans Godot. Vérifier :
  - La question s'affiche
  - Les hints apparaissent lors du swipe partiel
  - La carte s'envole vers la gauche ou droite au release
  - `choice_made` est bien émis

- [ ] **Step 4 : Committer**

  ```bash
  git add src/ui/CardScreen.gd scenes/CardScreen.tscn
  git commit -m "feat(ui): implement CardScreen with swipe animation"
  ```

---

## Task 4 : DeathScreen

**Files:**
- Create: `~/foundation-reigns/src/ui/DeathScreen.gd`
- Create: `~/foundation-reigns/scenes/DeathScreen.tscn`

- [ ] **Step 1 : Créer la scène DeathScreen.tscn**

  ```
  DeathScreen (Control, full screen, fond noir semi-transparent)
  ├── Title (Label) ← "Speaker [Nom] — [Âge] ans"
  ├── SubTitle (Label) ← "Couverture : [couverture] — Cause : [cause]"
  ├── ScrollContainer
  │   └── VBoxContainer
  │       ├── TimelineLabel (RichTextLabel) ← crises / quêtes
  │       ├── GalaxyStateLabel (RichTextLabel) ← ressources / factions
  │       └── LegacyLabel (RichTextLabel) ← toKeep transmis
  ├── SeldonMessage (PanelContainer)
  │   └── SeldonText (Label) ← message holographique
  └── ContinueButton (Button) ← "Nouveau règne"
  ```

- [ ] **Step 2 : Créer DeathScreen.gd**

  ```gdscript
  class_name DeathScreen
  extends Control

  signal continue_pressed

  @onready var _title         = $Title
  @onready var _subtitle      = $SubTitle
  @onready var _timeline      = $ScrollContainer/VBoxContainer/TimelineLabel
  @onready var _galaxy_state  = $ScrollContainer/VBoxContainer/GalaxyStateLabel
  @onready var _legacy        = $ScrollContainer/VBoxContainer/LegacyLabel
  @onready var _seldon_text   = $SeldonMessage/SeldonText
  @onready var _continue_btn  = $ContinueButton

  func _ready() -> void:
      _continue_btn.pressed.connect(func(): continue_pressed.emit())

  func show_death(ctx: Context, death_type: String, cover_name: String) -> void:
      var speaker_name = ctx.get_var("speaker_name", "Inconnu")
      var age = ctx.get_var("age", 50)

      # En-tête
      _title.text = "Speaker %s — %d ans" % [speaker_name, age]
      var cause_text = _death_type_to_fr(death_type)
      _subtitle.text = "Couverture : %s  |  Cause : %s" % [cover_name, cause_text]

      # Timeline (crises + jalons)
      var timeline = ""
      for i in range(1, 7):
          var key = "seldon_crisis_%d" % i
          var val = ctx.get_var(key, 0)
          if val == 1:
              timeline += "✓ Crise de Seldon %d — traversée\n" % i
          elif val == -1:
              timeline += "✗ Crise de Seldon %d — ratée\n" % i
      _timeline.text = timeline if timeline != "" else "Aucune crise de Seldon atteinte ce règne."

      # État de la galaxie
      var resources = ["military", "religion", "commerce", "politics"]
      var resource_names = {"military": "Militaire", "religion": "Religion",
                            "commerce": "Commerce",  "politics": "Politique"}
      var galaxy = ""
      for r in resources:
          galaxy += "%s : %d\n" % [resource_names[r], ctx.get_var(r, 50)]
      galaxy += "\nAnnée galactique : %d" % ctx.get_var("year", 1)
      _galaxy_state.text = galaxy

      # Héritage transmis
      var legacy = "Variables toKeep transmises :\n"
      for key in ctx._keep_flags:
          legacy += "  • %s = %s\n" % [key, str(ctx.get_var(key))]
      _legacy.text = legacy

      # Message de Seldon
      _seldon_text.text = _get_seldon_message(ctx, death_type)

  func _death_type_to_fr(death_type: String) -> String:
      match death_type:
          "natural":   return "Mort naturelle"
          "resource":  return "Effondrement d'une ressource"
          "exposed":   return "Couverture démasquée"
          _:           return "Inconnue"

  func _get_seldon_message(ctx: Context, death_type: String) -> String:
      var year = ctx.get_var("year", 1)
      var crises_passed = 0
      for i in range(1, 7):
          if ctx.get_var("seldon_crisis_%d" % i, 0) == 1:
              crises_passed += 1

      if death_type == "natural":
          return "Vous avez servi jusqu'à la fin de vos jours. Le Plan vous remercie."
      if crises_passed >= 3:
          return "An %d. Le Plan dévie de moins de 2%%. Votre successeur a une bonne marge." % year
      if crises_passed == 0:
          return "An %d. Le Plan dévie. La correction sera difficile. Mais pas impossible." % year
      return "An %d. Les fondations tiennent. Continuez." % year
  ```

- [ ] **Step 3 : Test visuel**

  Dans une scène test, appeler `show_death()` avec un `Context` peuplé.
  Vérifier que toutes les zones s'affichent correctement.

- [ ] **Step 4 : Committer**

  ```bash
  git add src/ui/DeathScreen.gd scenes/DeathScreen.tscn
  git commit -m "feat(ui): implement DeathScreen with timeline and Seldon message"
  ```

---

## Task 5 : GalaxyMap

**Files:**
- Create: `~/foundation-reigns/src/ui/GalaxyMap.gd`
- Create: `~/foundation-reigns/scenes/GalaxyMap.tscn`
- Create: `~/foundation-reigns/assets/galaxy_placeholder.png` (image 800x600 noire avec points blancs)

- [ ] **Step 1 : Créer le placeholder galaxy map**

  Créer une image simple dans Godot (ou utiliser un PNG noir 800x600 avec 12 points de couleur).

  Dans le terminal, créer un placeholder SVG converti en PNG :
  ```bash
  python3 -c "
  import os
  # Create a minimal 800x600 PNG placeholder (black with white dots)
  # We use a simple Python struct approach for a tiny valid PNG
  print('Create assets/galaxy_placeholder.png manually in Godot ImportEditor')
  "
  ```

  Alternative : dans Godot, créer `assets/galaxy_placeholder.png` via l'éditeur d'images (800×600, fond noir, 12 cercles de couleur pour les planètes).

- [ ] **Step 2 : Créer la scène GalaxyMap.tscn**

  ```
  GalaxyMap (Control, popup plein écran)
  ├── Background (TextureRect, 800x600, galaxy_placeholder.png)
  ├── PlanetsContainer (Node2D)
  │   ├── Terminus   (Button, position approximative sur la carte)
  │   ├── Trantor    (Button)
  │   ├── Anacreon   (Button)
  │   ├── ... (12 boutons au total)
  └── PlanetPopup (PanelContainer, hidden par défaut)
      ├── PopupName    (Label)
      ├── PopupFaction (Label)
      ├── PopupState   (Label)
      └── CloseButton  (Button)
  ```

  Positions approximatives des planètes (à ajuster selon l'illustration finale) :
  ```
  Terminus   : (400, 300)   ← centre
  Trantor    : (350, 150)
  Anacreon   : (200, 200)
  Santanni   : (150, 280)
  Smyrno     : (100, 350)
  Askone     : (500, 400)
  Korell     : (600, 300)
  Siwenna    : (300, 100)
  Kalgan     : (550, 180)
  Neotrantor : (450, 80)
  Rossem     : (700, 200)
  Sayshell   : (650, 450)
  ```

- [ ] **Step 3 : Créer GalaxyMap.gd**

  ```gdscript
  class_name GalaxyMap
  extends Control

  const COLOR_ALLIED  = Color(0.2, 0.8, 0.2)   # vert
  const COLOR_NEUTRAL = Color(0.6, 0.6, 0.6)   # gris
  const COLOR_HOSTILE = Color(0.8, 0.2, 0.2)   # rouge

  const PLANET_IDS = [
      "terminus", "trantor", "anacreon", "santanni", "smyrno",
      "askone", "korell", "siwenna", "kalgan", "neotrantor",
      "rossem", "sayshell"
  ]

  @onready var _planets_container = $PlanetsContainer
  @onready var _popup       = $PlanetPopup
  @onready var _popup_name  = $PlanetPopup/PopupName
  @onready var _popup_faction = $PlanetPopup/PopupFaction
  @onready var _popup_state = $PlanetPopup/PopupState
  @onready var _close_btn   = $PlanetPopup/CloseButton

  var _game_data: FoundationGameData

  func _ready() -> void:
      _close_btn.pressed.connect(func(): _popup.hide())
      _popup.hide()

  func setup(game_data: FoundationGameData) -> void:
      _game_data = game_data

  func update(ctx: Context) -> void:
      for planet_id in PLANET_IDS:
          var btn = _planets_container.get_node_or_null(planet_id.capitalize())
          if not btn:
              continue
          var state = ctx.get_var("planet_%s_state" % planet_id, 0)
          _set_planet_color(btn, state)

  func _set_planet_color(btn: Button, state: int) -> void:
      var color = COLOR_NEUTRAL
      match state:
           1: color = COLOR_ALLIED
          -1: color = COLOR_HOSTILE
      var style = StyleBoxFlat.new()
      style.bg_color = color
      style.corner_radius_top_left    = 8
      style.corner_radius_top_right   = 8
      style.corner_radius_bottom_left = 8
      style.corner_radius_bottom_right = 8
      btn.add_theme_stylebox_override("normal", style)

  func _on_planet_pressed(planet_id: String, ctx: Context) -> void:
      if not _game_data:
          return
      var planet = _game_data.get_planet_by_id(planet_id)
      if planet.is_empty():
          return

      var state = ctx.get_var("planet_%s_state" % planet_id, 0)
      var state_text = {1: "Alignée ✓", 0: "Neutre", -1: "Hostile ✗"}

      _popup_name.text    = planet.get("name", planet_id)
      _popup_faction.text = "Faction : " + planet.get("faction", "?")
      _popup_state.text   = "État : " + state_text.get(state, "?")
      _popup.show()
  ```

- [ ] **Step 4 : Test visuel**

  Lancer la scène GalaxyMap avec un Context qui a des états de planètes variés.
  Vérifier que les couleurs sont correctes et que le popup s'ouvre au clic.

- [ ] **Step 5 : Committer**

  ```bash
  git add src/ui/GalaxyMap.gd scenes/GalaxyMap.tscn assets/
  git commit -m "feat(ui): implement GalaxyMap with planet state colors"
  ```

---

## Task 6 : Main.gd — boucle principale

**Files:**
- Create: `~/foundation-reigns/src/main/Main.gd`
- Create: `~/foundation-reigns/scenes/Main.tscn`

- [ ] **Step 1 : Créer la scène Main.tscn**

  ```
  Main (Node)
  ├── GameData (FoundationGameData — autoload ou node)
  ├── CardScreen (instance CardScreen.tscn)
  ├── DeathScreen (instance DeathScreen.tscn, hidden)
  ├── GalaxyMap (instance GalaxyMap.tscn, hidden)
  └── MapButton (Button, "Carte Galactique")
  ```

- [ ] **Step 2 : Créer Main.gd**

  ```gdscript
  extends Node

  var _game_data: FoundationGameData
  var _ctx: Context
  var _model: NarrativeModel
  var _legitimacy: LegitimacySystem
  var _respawn: RespawnSystem
  var _save: SaveSystem

  @onready var _card_screen  = $CardScreen
  @onready var _death_screen = $DeathScreen
  @onready var _galaxy_map   = $GalaxyMap
  @onready var _map_button   = $MapButton

  var _current_card: Dictionary = {}
  var _awaiting_reaction: bool = false

  func _ready() -> void:
      _game_data = FoundationGameData.new()
      if not _game_data.load_all():
          push_error("Main: failed to load game data")
          return

      _ctx       = Context.new()
      _save      = SaveSystem.new()
      _legitimacy = LegitimacySystem.new(_ctx)
      _respawn   = RespawnSystem.new(_ctx)

      if _save.has_save():
          _save.load(_ctx)
      else:
          _ctx.initialize_new_reign()
          _ctx.set_var("year", 1, true)
          _ctx.set_var("age", 35 + randi() % 6)

      _model = NarrativeModel.new(_game_data, _ctx)
      _galaxy_map.setup(_game_data)

      _card_screen.choice_made.connect(_on_choice_made)
      _death_screen.continue_pressed.connect(_on_new_reign)
      _map_button.pressed.connect(_on_map_pressed)

      _next_card()

  func _next_card() -> void:
      _current_card = _model.draw_card()
      if _current_card.is_empty():
          push_error("Main: no card to draw")
          return

      # Apply loadOutcome
      var load_outcomes = _current_card.get("loadOutcome", [])
      _model.apply_outcomes(load_outcomes)

      _card_screen.show_card(_current_card, _ctx)
      _awaiting_reaction = false

  func _on_choice_made(is_left: bool) -> void:
      if _awaiting_reaction:
          return
      _awaiting_reaction = true

      # Apply outcomes
      var outcome_key = "yesOutcome" if is_left else "noOutcome"
      var outcomes = _current_card.get(outcome_key, [])
      _model.apply_outcomes(outcomes)

      # Apply legitimacy outcome if any
      for outcome in outcomes:
          if outcome.get("variable") == "legitimacy":
              pass  # already applied via apply_outcomes

      # Show reaction text
      _card_screen.show_reaction(_current_card, is_left)
      _model.mark_card_seen(_current_card)

      # Increment turns
      _ctx.add_var("turns", 1)

      # Save
      _save.save(_ctx)

      # Check game over
      if _ctx.is_game_over():
          await get_tree().create_timer(1.5).timeout
          _show_death_screen(_ctx.get_game_over_reason())
          return

      # Check legitimacy exposed
      if _legitimacy.is_exposed():
          await get_tree().create_timer(1.5).timeout
          _show_death_screen("exposed")
          return

      # Check natural death
      var age = _ctx.get_var("age", 35)
      if _should_die_naturally(age):
          await get_tree().create_timer(1.5).timeout
          _show_death_screen("natural")
          return

      # Next card after short delay
      await get_tree().create_timer(1.2).timeout
      _next_card()

  func _should_die_naturally(age: int) -> bool:
      var prob = 0.0
      if    age >= 83: prob = 1.0
      elif  age >= 81: prob = 0.60
      elif  age >= 79: prob = 0.35
      elif  age >= 77: prob = 0.15
      elif  age >= 75: prob = 0.05
      else: return false
      return randf() < prob

  func _show_death_screen(cause: String) -> void:
      var cover = _ctx.get_var("cover_name", "Inconnu")
      _death_screen.show_death(_ctx, cause, cover)
      _death_screen.show()
      _card_screen.hide()

  func _on_new_reign() -> void:
      var death_type = "resource"
      _respawn.respawn(death_type)
      _model = NarrativeModel.new(_game_data, _ctx)
      _death_screen.hide()
      _card_screen.show()
      _next_card()

  func _on_map_pressed() -> void:
      _galaxy_map.update(_ctx)
      _galaxy_map.show()
  ```

- [ ] **Step 3 : Définir la scène principale dans project.godot**

  Dans Godot : `Project → Project Settings → Application → Run → Main Scene`
  Sélectionner `res://scenes/Main.tscn`

- [ ] **Step 4 : Test du cycle complet**

  Lancer le jeu. Vérifier :
  - Une carte s'affiche au démarrage
  - Le swipe à gauche/droite déclenche les outcomes
  - Les barres de ressources se mettent à jour
  - La réaction s'affiche brièvement
  - La carte suivante apparaît
  - Un game over à military=0 → Death Screen
  - Le bouton "Nouveau règne" relance le cycle

- [ ] **Step 5 : Committer**

  ```bash
  git add src/main/Main.gd scenes/Main.tscn
  git commit -m "feat(main): implement complete game loop with death/respawn"
  ```

---

## Livrable Phase 3

À la fin de cette phase, le jeu est **jouable de bout en bout** :

```
Démarrage → Carte s'affiche → Swipe → Outcomes → Barres mises à jour
         → Nouvelle carte → ... → Game Over → Death Screen → Nouveau règne
```

Toutes les scènes fonctionnent. Phase 4 (contenu) peut commencer.
